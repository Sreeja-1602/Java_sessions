class SubString{
    public static void main(String[] args){
        public List<String> stringMatching(String[] words){
            List<String> list = new ArrayList<String>();
            for(int i =0;i<words.length;i++){
                String stringOne = words[i];
                for(int j=0;j<words.length;j++){
                    String stringTwo = words[j];
                if( i !== j && words[i].length() - sOne.length< stringTwo.length())
                if(checkIfSubString(string One,string Two) == true){
                    list.add(string.One);
                }

                }
            }
        }
        return list; 
    }
           public boolean checkIfString (String sOne, string sTwo){
            for(int i =0;i < sTwo.length();i++){
                if(sOne.equals(sTwo.subString(i, sOne.length()+i)))
            }
           }
           return false;
    }
}